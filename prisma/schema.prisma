generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model MgnregaData {
  state      String
  district   String
  month      String
  households Int
  workdays   Int
  wages      Decimal

  @@id([state, district, month])
  @@map("mgnrega_data")
}
